# dependencies / build output
node_modules/
dist/

# runtime state (world save — delete-able, never commit)
server/save.json
gameserver/world.save.json
gameserver/world.*.save.json

# logs & scratch
*.log
srv.log
vite.log

# environment / secrets
.env
.env.*
# derived-userId HMAC key: machine-local, and rotating it orphans every save
control/.id-secret

# editor & OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
